\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{11}          % (optional) specify the session number
\sessiondate{10/6/21}   % the date of the session

\warmup{Concept Review}
\topic{Inverse Trig Rules}
\topic{Implicit Differentiation}
\cooldown{Review}

% Worksheet Information
\title{Implicit Differentiation, and \linebreak Inverse Trigonometric Rules}
\sections{Section 3.8-3.9a}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Concept Review}{Try these problems \textbf{alone} as your peers join the session. Do your best to not refer to your notes.}{15 minutes}
    
    \mcq[2]{Compute the derivative.}{
        \task $\dddx\left[\arcsin(x)\right]=$
        \tinysp
        \task $\dddx\left[\arccos(x)\right]=$
        \task $\dddx\left[\arcsec(x)\right]=$
        \tinysp
        \task $\dddx\left[\arccsc(x)\right]=$
        \task $\dddx\left[\arctan(x)\right]=$
        \tinysp
        \task $\dddx\left[\arccot(x)\right]=$
    }

    \mcq[2]{Compute the derivative.}{
        \task $\dddx\left[b^x\right]=$
        \tinysp
        \task $\dddx\left[\ln(x)\right]=$
        \task $\dddx\left[e^x\right]=$
        \tinysp
        \task $\dddx\left[\log_b(x)\right]=$
    }

    \frq{Find an equation of the tangent line to the implicit equation at the point $(2,1)$.}
    $$xy+x^2y^2=6$$
    \pagebreak
    
    \activity{Activity 1}{Implicit Differentiation}{Work together in your \textbf{breakout rooms} to answer these questions. Do your best to not refer to your notes while working on these problems.}{30 minutes}
    
    \frq{Find $\dddf[a]{b}$ when $ae^b=2ab+b^3$.}
    \Hugesp
    \frq{Find $\dddf[b]{a}$ when $ae^b=2ab+b^3$.}
    \newpage
    \frq{Find $\dddf[z]{t}$ when $\tan(t^2z)=(t+z)^3$.}
    \Hugesp
    \frq{Prove that $\dddx[y]=-\dfrac{1}{x^2(\frac12y^{-1/2}-2y)}$ when $\sqrt{y}=\dfrac1x+y^2$.}
    \newpage 
\end{document}