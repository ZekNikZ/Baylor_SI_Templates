\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{10}         % (optional) specify the session number
\sessiondate{2/20/20}   % the date of the session

\warmup{Rule Review}
\topic{Trigonometric Rules}
\topic{Chain Rule}

% Worksheet Information
\title{Trigonometric Derivative Rules\linebreak and the Chain Rule}
\sections{Sections 3.6 and 3.7}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Rule Review}{Work with a \textbf{partner} to answer these questions. Try not to use your notes.}{15 minutes}
    
    \frq{What is the chain rule?}
    \Tinysp
    \mcq[2]{Give the following derivatives.}{
        \task $\dddx\sin(x)=$
        \tinysp
        \task $\dddx\cos(x)=$
        \task $\dddx\sec(x)=$
        \tinysp
        \task $\dddx\csc(x)=$
        \task $\dddx\tan(x)=$
        \tinysp
        \task $\dddx\cot(x)=$
    }
    \pagebreak
    
    \activity{Activity 1}{Computing Derivatives}{Make a \textbf{group of two or three, all with different colored worksheets,} to answer these questions. Try not to use your notes.}{30 minutes}
    
    \frq{Find the first, second, third, and fourth derivatives of $f(\theta)=\cos \theta$.}
    \newpage
    \mcq{Find the derivative of the following functions.}{
        \task $g(x)=\sec(x)\tan(x)$
        \Normalsp
        \task $h(x)=9\csc x+\dfrac{\cot x}{x}$
        \Normalsp
        \task $y=(x^3-4x+9e^{-x/9})^{-4/3}$
        \Normalsp
        \task $u(t)=\sin(t^3)+\sec^3(t)$
        \Normalsp
        \task $f(x)=(e^x+2)^4(3x-1)^2$
        \Normalsp
    }
    \frq{Compute the derivative of $f(g(x))$ and $g(f(u))$.}
    $$f(u)=\cos(u)~~~~~~~g(x)=x^2+1$$
    \newpage
    \frq{Compute the derivative of $f \circ g$.}
    $$f(u)=\frac{u}{u-1}~~~~~~~g(x)=\csc x$$
    \Smallsp

\end{document}