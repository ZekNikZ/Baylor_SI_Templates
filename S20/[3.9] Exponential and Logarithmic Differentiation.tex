\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{12}          % (optional) specify the session number
\sessiondate{2/27/19}   % the date of the session

\warmup{Concept Review}
\topic{Derivatives of Exponential Functions}
\topic{Derivatives of Logarithmic Functions}
\cooldown{Related Rates}

% Worksheet Information
\title{Implicit Differentiation, and \linebreak Inverse Trigonometric Rules}
\sections{Section 3.9}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Concept Review}{Work \textbf{alone} then share your answers with a \textbf{partner}. Try not to use your notes.}{15 minutes}
    
    \mcq[2]{Compute the derivative.}{
        \task $\dddx\left[b^x\right]=$
        \tinysp
        \task $\dddx\left[\ln(x)\right]=$
        \task $\dddx\left[e^x\right]=$
        \tinysp
        \task $\dddx\left[\log_b(x)\right]=$
        \task $\dddx\left[e^{\pi x}\right]=$
        \tinysp
        \task $\dddx\left[\ln(ex)\right]=$
    }

    \mcq[2]{Simplify.}{
        \task $\ln(e^{3x})=$
        \tinysp
        \task $e^{\ln(2x)}=$
        \task $e^{g(x)\ln(f(x))}=$
        \tinysp
        \task $\ln(e)=$
    }

    \frq{Find $\dddx[y]$ when $x^4+\sin(xy)-2e^x+y=e$.}
    
    \pagebreak
    \activity{Activity 1}{Derivatives of Exponential and Logarithmic Functions}{Work in a \textbf{group of 2 or 3 people sitting near you} to answer these questions. Try not to use your notes.}{30 minutes}

    \begin{multipartquestion} % 4 log (easy, chain, chain with input, log_a)
        Compute the following derivatives. Do not use a calculator.
        \frq{$\dddx\left[e^x\ln(x)\right]$}
        \normalsp
        \frq{$\dddf{t}\left[\ln(t5^t)\right]$}
        \normalsp
        \frq{$\dddf{t}\left[\log_3(\sin(t))\right]$}
        \Normalsp
    \end{multipartquestion}

    \newpage
    \begin{multipartquestion} % 2 exp (chain, chain with input)
        Compute the following derivatives. Do not use a calculator.
        \frq{$\dddx\left[2^{2^x}\right]$}
        \Normalsp
        \frq{$\dddx\left[x\ln(e^{3x})\right]$}
        \Normalsp
        \frq{$\dddx[y]\Bigr|_{x=\ln(6)}$ when $y=e^{2x}$}
        \newpage
    \end{multipartquestion}

    \activity{Activity 2}{Harder Derivatives}{Work in a \textbf{group of 2 or 3 people with the same color worksheet as you} to answer these questions. Try not to use your notes.}{30 minutes}
    
    \frq{Compute the derivative of $g(w)=(3w-7)^{4w}$.}
    \Hugesp
    \frq{Compute the derivative of $f(x)=(2x-e^{8x})^{\sin(2x)}$.}
    \Hugesp
    \frq{Compute the derivative of $v(t)=\sin^{-1}(t\ln(t))$}
    \Hugesp
    \frq{Prove that $\dddx[y]=-\dfrac{1}{x^2(\frac12y^{-1/2}-2y)}$ when $\sqrt{y}=\dfrac1x+y^2$.}
    \newpage
    
    \activity{Cooldown}{Related Rates}{Test your knowledge of previous topics by answering these questions \textbf{alone}. Try not to use your notes.}{15 minutes}
    \mcq{For a certain rectangle, the length of one side is always three times the length of the other side.}{
        \task If the shorter side is decreasing at a rate of 2 inches/minute at what rate is the longer side decreasing?
        \task At what rate is the enclosed area decreasing when the shorter side is 6 inches long and is decreasing at a rate of 2 inches/minute?
    }

\end{document}