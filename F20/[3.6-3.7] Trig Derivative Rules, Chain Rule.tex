\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{11}         % (optional) specify the session number
\sessiondate{2/20/20}   % the date of the session

\warmup{Rule Review}
\topic{Trigonometric Rules}
\topic{Chain Rule}

% Worksheet Information
\title{Trigonometric Derivative Rules\linebreak and the Chain Rule}
\sections{Sections 3.6 and 3.7}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Rule Review}{Try these problems \textbf{alone} as your peers join the session. Do your best to not refer to your notes.}{15 minutes}
    
    \frq{What is the chain rule?}
    \Tinysp
    \mcq[2]{Give the following derivatives.}{
        \task $\dddx\sin(x)=$
        \tinysp
        \task $\dddx\cos(x)=$
        \task $\dddx\sec(x)=$
        \tinysp
        \task $\dddx\csc(x)=$
        \task $\dddx\tan(x)=$
        \tinysp
        \task $\dddx\cot(x)=$
    }
    \pagebreak
    
    \activity{Activity 1}{Computing Derivatives}{Work together in your \textbf{breakout rooms} to answer these questions. Do your best to not refer to your notes while working on these problems.}{30 minutes}
    
    \frq{Find the first, second, third, and fourth derivatives of $f(\theta)=\cos \theta$.}
    \Hugesp
    \mcq{Find the derivative of the following functions.}{
        \task $g(x)=\sec(x)\tan(x)$
        \Normalsp
        \task $h(x)=9\csc x+\dfrac{\cot x}{x}$
        \Normalsp
        \task $u(t)=\sin(t^3)+\sec^3(t)$
        \Normalsp
        \task $f(x)=x^2\sin x+2x\cos x-2\sin x$
    }
    \newpage

    \activity{Activity 2}{Chain Rule}{Work together in your \textbf{breakout rooms} to answer these questions. Do your best to not refer to your notes while working on these problems.}{30 minutes}
    \mcq{Find the derivative of the following functions.}{
        \task $y=(x^3-4x+9e^{-x/9})^{-4/3}$
        \Normalsp
        \task $f(x)=(e^x+2)^4(3x-1)^2$
        \Normalsp
        \task $g(x)=2\sin(3x+\tan(x))$
        \Normalsp
        \tinysp
        \task $s(t)=\csc^2(t+t^2)$
    }
    \Normalsp
    \frq{Compute the derivative of $f(g(x))$ and $g(f(u))$.}
    $$f(u)=\cos(u)~~~~~~~g(x)=x^2+1$$
    \newpage
    \frq{Compute the derivative of $f \circ g$ and $g \circ f$.}
    $$f(u)=\frac{u}{u-1}~~~~~~~g(x)=\csc x$$
    \newpage
    
    \activity{Cooldown}{Chain Rule Challenge}{Attempt to do these problems \textbf{alone} then discuss your peers.}{30 minutes}
    \frq{Compute the derivative of the following function.}
    $$f(x)=xe^{\sin\left(e^{\left(x+x^2\right)}\right)}$$

\end{document}