\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{11}         % (optional) specify the session number
\sessiondate{2/20/20}   % the date of the session

\warmup{Rule Review}
\topic{Trigonometric Rules}
\topic{Chain Rule}
\topic{Implicit Differentiation}

\newcommand{\inside}{\mathbf{\color{blue} x^2+2}}

% Worksheet Information
\title{Trig. Derivatives, Chain Rule, \linebreak Intro to Implicit Differentiation}
\sections{Sections 3.6–3.8a}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Rule Review}{Try these problems \textbf{alone} as your peers join the session. Do your best to not refer to your notes.}{7 minutes}
    
    \frq{What is the chain rule?}
    \Tinysp
    \mcq[2]{Give the following derivatives.}{
        \task $\dddx\sin(x)=$
        \tinysp
        \task $\dddx\cos(x)=$
        \task $\dddx\sec(x)=$
        \tinysp
        \task $\dddx\csc(x)=$
        \task $\dddx\tan(x)=$
        \tinysp
        \task $\dddx\cot(x)=$
    }
    \mcq[2]{Compute the following derivatives.}{
        \task $\left[\cos(x^2)\right]'$
        \task $\dddx\left[e^{x^2+1}\right]$
    }
    \pagebreak
    
    \activity{Activity 1}{Computing Derivatives}{Work together in your \textbf{breakout rooms} to answer these questions. Do your best to not refer to your notes while working on these problems.}{10 minutes}
    
    \frq{Compute the derivative of $f(x)=\dfrac{e^x+3}{x}$ \textbf{without using the quotient rule.}}
    \Hugesp
    \mcq{Find the derivative of the following functions.}{
        \task $h(x)=9\csc x+\dfrac{\cot x}{x}$
        \Normalsp
        \task $f(x)=x^2\sin x+2x\cos x-2\sin x$
        \Normalsp
        \task $u(t)=\sin(t^3)+\sec^3(t)$
        \Normalsp
        \task $g(x)=e^{\sec(x)\tan(x)}$
    }
    \newpage
    \frq{Compute the derivative of $f(g(x))$ and $g(f(u))$.}
    $$f(u)=\cos(u)~~~~~~~g(x)=x^2+1$$
    \newpage

    \activity{Activity 2}{Chain Rule and Implicit Differentiation}{Work together in your \textbf{breakout rooms} to answer these questions. Do your best to not refer to your notes while working on these problems.}{20 minutes}
    \mcq{Find the derivative of the following functions.}{
        \task $y=(x^3-4x+9e^{-x/9})^{-4/3}$
        \Normalsp
        \task $f(x)=(e^x+2)^4(3x-1)^2$
        \Normalsp
        \task $g(x)=2\sin(3x+\tan(x))$
        \Normalsp
        \tinysp
        \task $s(t)=\csc^2(t+t^2)$
        \Normalsp
    }
    \mcq{Find $\dddx y$.}{
        \task $\sin(y)=\cos(x)$
        \Normalsp
        \task $y^2=e^y+x$
    }
    \newpage
    
    \activity{Cooldown}{Chain Rule Challenge}{Attempt to do these problems \textbf{alone} then discuss your peers.}{5 minutes}
    \frq{Compute the derivative of the following function.}
    $$f(x)=e^{\sin\left(e^{\left(x+x^2\right)}\right)}$$
    \Hugesp
    
    \frq{Find an equation of the tangent line to the following function at $x=\dfrac{\pi}{3}$.}
    $$y=\sec(x)$$
    \newpage
    
    \thispagestyle{empty} 
    \begin{center}
        {\Large Identifying the ``Inside'' Function}
        
        {\large Below are several examples of functions that would require the use of the chain rule when taking the derivative. Expressions in \textbf{\color{blue} blue} are the ``inside'' function.}
    \end{center}
    \tinysp

    \Large
    \begin{multicols}{3}\noindent
        $$(\inside)^{3/2}$$
        \tinysp
        $$e^{\inside}$$
        \tinysp
        $$7^{\inside}$$
        \tinysp
        $$\sqrt{\inside}$$
        \columnbreak
        $$\ln(\inside)$$
        \tinysp
        $$\log_2(\inside)$$
        \tinysp
        $$\dfrac{1}{\inside}$$
        \columnbreak
        $$\sin(\inside)$$
        \tinysp
        $$\arccos(\inside)$$
        \tinysp
        $$\sec^{-1}(\inside)$$
        \tinysp
        $$\mathbf{\color{blue}\sec}^2\mathbf{\color{blue}(}\mathbf{\color{red}x^2+2}\mathbf{\color{blue})}=\left(\mathbf{\color{blue}\sec(}\mathbf{\color{red}x^2+2}\mathbf{\color{blue})}\right)^2$$
    \end{multicols}

\end{document}