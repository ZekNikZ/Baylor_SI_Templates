\documentclass{siproblemset}

% SI Session Information
\course{MTH 1321}       % the course of your SI
\sessionnum{12}          % (optional) specify the session number
\sessiondate{10/9/19}   % the date of the session

\warmup{Concept Review}
\topic{Exponential Derivatives}
\topic{Logarithmic Derivatives}
\cooldown{Test Review}

% Worksheet Information
\title{Exponential and \linebreak Logarithmic Derivatives}
\sections{Section 3.9}
\withnamespace

\begin{document}
    \maketitle
    \activity{Warmup}{Concept Review}{Work with a \textbf{partner} to answer these questions. Try not to use your notes.}{15 minutes}
    
    \mcq[2]{Find the derivatives using any method.}{
        \task $\dddx\left[e^{2x}\right]=$
        \Normalsp
        \task $\dddx\left[13^{\sin x}\right]=$
        \task $\dddx\left[\ln(x^2)\right]=$
        \Normalsp
        \task $\dddx\left[\log_7(x+2)\right]=$
    }
    
    \pagebreak
    \activity{Activity 1}{Exponential Derivatives}{Make a \textbf{group of two or three, all with the same colored worksheets,} to answer your assigned question. Try not to use your notes.}{30 minutes}
    
    \mcq{Find the derivatives using derivative rules \underline{\textbf{and}} by rewriting the equation using the identity $x=e^{\ln x}$.}{
        \task $f(x)=7^{4x-x^2}$
        \hugesp
        \task $y=16^{\sin x}$
        \Hugesp
        \task $g(x)=3^{(\ln(x))^2}$
        \hugesp
        \task $y=\dfrac{2^x-3^{-x}}{x}$ (only need to use derivative rules)
        \Hugesp
    }
    \newpage
    
    \mcq{Find the derivative by rewriting the equation using the identity $x=e^{\ln x}$.}{
        \task $y=x^{\cos x}$
        \Hugesp
        \task $y=(e^x)^{\sec x}$
        \Hugesp
    }
\newpage

    \activity{Activity 2}{Logarithmic Derivatives}{Make a \textbf{{\em new} group of three, all with the different colored worksheets,} to answer these questions. Try not to use your notes.}{30 minutes}
    
    \mcq{Find the derivatives using derivative rules \underline{\textbf{and}} by rewriting the equation by applying the identity $x=b^{\log_b x}$ to both sides.}{
        \task $R(z)=\log_5(2z^2+7)$
        \hugesp
        \task $y=\ln(\cot x)$
        \Hugesp
    }

    \mcq{Find the derivatives using derivative rules \underline{\textbf{and}} by rewriting the equation by taking the logarithm of both sides.}{
        \task $y=(x-1)(x-12)(x-7)$
        \hugesp
        \task $f(x)=\dfrac{x(x+1)^3}{(3x-1)^2}$
        \Hugesp
    }
    \newpage
    
    \mcq{Find the derivative by rewriting the equation by taking the logarithm of both sides.}{
        \task $y=x^{\cos x}$
        \Largesp
        \task $y=(e^x)^{\sec x}$
        \Largesp
    }
    
    \newpage
    \activity{Cooldown}{Applying Exponential and Logarithmic Derivatives}{Attempt to do these problems \textbf{alone} then discuss your answers with the people around you.}{15 minutes}
    
    \frq{Find an equation of the tangent line to $g$ at $t=2$ when $g(t)=\log_4(t4^t)$.}
    \Hugesp
    
    \frq{Find $f'(x)$ using any method: $f=\left(\ln\left(\ln x\right)\right)^3$.}
    
    
\end{document}