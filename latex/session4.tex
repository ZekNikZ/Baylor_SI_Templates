\documentclass{siproblemset}

\usepackage{multicol}
\usepackage{xcolor}

% SI Session Information
\course{MTH 1321}
\sessionnum{5}
\sessiondate{9/16/19}

\warmup{Concept Review}
\topic{Trig Limits Review}
\topic{Limits at Infinity}
\topic{Finding Horizontal Asymptotes}
\cooldown{Review and Preview}

% Worksheet Information
\title{Trig Limit Review and\linebreak Limits at Infinity}
\sections{Section 2.7}
\withnamespace

\begin{document}
    \maketitle
    
    \activity{Warmup}{Concept Overview}{Work with a \textbf{partner} to answer these questions. Try not to use your notes.}{20 minutes}
    
    \frq{What is the difference between a limit ``at'' infinity and a limit ``being (equal to)'' infinity?}
    \nospace
    
    \mcq[2]{Evaluate the following limits.}{
        \task $\lim\limits_{x\rightarrow0}\dfrac{\sin x}{x}$
        \task $\lim\limits_{x\rightarrow0}\dfrac{1-\cos x}{x}$
    }
    \mcq[2]{Evaluate both $\lim\limits_{x\rightarrow-\infty}f(x)$ and $\lim\limits_{x\rightarrow\infty}f(x)$.}{
        \task $f(x)=e^x$
        \task $f(x)=x^n$; $n$ is a positive whole number
        \smallspace
        \task $f(x)=\log x$
        \task $f(x)=1/x$
        \smallspace
        \task $f(x)=\sqrt{x}$
        \task $f(x)=\sin(\frac1x)$
    }
    
    \pagebreak
    \activity{Activity 1}{Trig Limits Review and Limits at Infinity}{Make a \textbf{group of three, all with different colored worksheets,} to answer these questions. Try not to use your notes.}{30 minutes}
    
    \frq{Evaluate the limit $\lim\limits_{\theta\rightarrow0}\dfrac{\sin5\theta}{\sin4\theta}$}
    \smallspace
    \frq{Evaluate the limit $\lim\limits_{t\rightarrow0}\dfrac{\sin(2t)-2\sin t}{t^2}$ (Hint: remember that $2\sin\theta\cos\theta=\sin2\theta$)}
    \smallspace
    \frq{Evaluate the limit $\lim\limits_{r\rightarrow\infty}\dfrac{9r^2}{6-r^8}$}
    \smallspace
    \frq{Find the horizontal asymptotes, if any of $f(x)=\dfrac{10}{1+3^{-x}}$}
    \smallspace
    \pagebreak

    \activity{Activity 2}{Limits at Infinity}{Make a \textbf{group of three, all with the same colored worksheet,} to answer these questions. Try not to use your notes.}{30 minutes}
    
    \frq{Evaluate the limit $\lim\limits_{x\rightarrow\infty}\left[\ln(3x+1)-\ln(2x+1)\right]$}
    \smallspace
    \frq{Evaluate the limit $\lim\limits_{u\rightarrow\infty}\dfrac{u^6+3u^2+5}{6-u^8}$}
    \smallspace
    \frq{Evaluate the limit $\lim\limits_{n\rightarrow\infty}\left[\sqrt{9n^4+16n}-3n^2\right]$}
    \smallspace
    \frq{Find the horizontal asymptotes, if any of $f(x)=\dfrac{\sqrt{49x^2-6}}{8x+4}$}
    \smallspace
    \pagebreak
    
    \activity{Cooldown}{Look Back, Look Forward}{Attempt to do these problems \textbf{alone} then discuss your answers with the people around you.}{15 minutes}
    
    \mcq[2]{Draw the graph of a function f(x) with the following properties:}{
        \task $\lim\limits_{x\rightarrow-3^-}f(x)=-\infty$
        \task $\lim\limits_{x\rightarrow-3^+}f(x)=2$
        \task $\lim\limits_{x\rightarrow\infty}f(x)=1$
        \task $f(x)$ has the horizontal asymptote $y=2$
        \task $f(x)$ has a removable discontinuity at\\$x=4$
        \task $f(x)$ has a jump discontinuity at $x=1$
    }
    \makebox[\width][c]{\begin{tikzpicture}[baseline=(current bounding box.north)]
        \begin{axis}[
        x=1.2cm,
        y=1.2cm,
        xmin=-6,
        xmax=6,
        ymin=-2,
        ymax=4,
        axis x line*=middle,
        axis y line*=middle,
        every axis plot/.append style={ultra thick},
        samples=60
        ]
        \end{axis}
    \end{tikzpicture}}

    \frq{For a continuous function, what y-values are guaranteed to be "hit" between x=a and x=b? Draw a graph if it aids your explanation.}
\end{document}